name = "heidelberger_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
heidelberger = "*"

[[pins]]
heidelberger = { path = ".." }

[build-profiles]
heidelberger       = "validation"
heidelberger_tests = "validation"
